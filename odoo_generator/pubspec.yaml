name: odoo_generator
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.0.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  odoo_annotation:
    path: ../odoo_annotation # o la versión publicada en pub.dev
  analyzer: ^7.0.0
  build: ^3.0.0
  source_gen: ^3.0.0

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^6.0.0
  test: ^1.24.0

publish_to: none
